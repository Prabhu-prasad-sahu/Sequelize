const { User } = require("../models/db")


let addUser = async (req,rsp)=>{
    try {
        const user = await emp.create({
            name : "Prabhuprasad"
        })
    } catch (error) {
        
    }
}